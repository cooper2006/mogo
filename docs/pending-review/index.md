# 待确认条目台账（index.md）

> 登记格式见 `README.md`。接线/偏离类决策需要用户拍板后，代理才执行对应实现改动。

| 文件路径 | 发现日期 | 发现者 | 疑点 | 建议 | 状态 |
|---|---|---|---|---|---|
| `docs/SDD增强功能核验报告.md` §4 缺口 1（007 网关韧性生产未接线） | 2026-09-25 | DSH Agent（SDD 核验轮） | failover/降级链/指数退避的库代码 + 39 项单测已落地，但生产 `InstrumentedLLMClient` 只接了 `estimate_cost`，三大韧性入口在 `app/` 内零生产调用点；真实 LLM 调用不触发韧性 | 二选一：① 在 `instrumented_client.py` 主调用路径接入 `run_with_degradation` + `FailoverScheduler` + `run_with_retry`（推荐，改动集中在 LLM 客户端层）；② 维持现状，在 README/007 spec 标注「库能力 + 单测，运行时接入为后续」 | resolved（2026-09-25：接线已落地——`configured_models.get_llm_client_by_model_id` 按同 main_id+capability 的 active 实例优先级构造 `ResilientLLMClient`，单实例 no-op（FR-9）；新增 6 项单测，`tests/llm` 53 项全绿） |
| `docs/SDD增强功能核验报告.md` §4 缺口 2（009 钩子引擎未挂载） | 2026-09-25 | DSH Agent（SDD 核验轮） | 钩子引擎 / 三规则 / 5s 预算 / fail-closed / 五事件 / 001 审计落点已落地 + 43 项测试，但 `turn_admission.py` 未 import hooks（引擎全仓零生产引用）；admin-web 无钩子规则页（SDD 声称「管理后台 → 钩子规则 `/api/hooks`」有 UI 入口） | ① 在 `turn_admission.py` 工具准入链挂载 `evaluate_pre_tool_use`（首期 PreToolUse，与 009 首期范围一致）；② 补 admin-web 钩子规则页（`/api/hooks` CRUD 已有，只差前端）；③ 若暂缓，在 SDD 对照表标注「引擎已落地，挂载/页面为后续」（§4 已部分标注，用户确认后定稿） | resolved（挂载+端点+UI，2026-09-25：`turn_admission.run_pre_tool_use` 已挂 PreToolUse 钩子门禁，`dsh_chat`/`dsh_execution` 传入 `tool="dsh_turn"` 上下文；`/api/hooks/rules` CRUD 端点已建；admin-web 钩子规则页（`/hooks/rules` 路由 + `HookRulesPage.vue` + `api/dsh_hooks.ts`）已落地，typecheck 通过；新增 18 项单测全绿） |
| `docs/SDD增强功能核验报告.md` §4 缺口 3（002 会话版本化无端点/UI） | 2026-09-25 | DSH Agent（SDD 核验轮） | `session_versioning` 包（commit 时间线 / 一次性 share 300s TTL / co-presence / 工作流版本化）已落地 + 49 项测试，但包内零外部引用、无 HTTP 端点、user-web/admin-web 无版本历史/分享/在线成员 UI | ① chat-api 补 `/api/sessions/{id}/commit|share|presence` 端点 + user-web 会话页三个 UI 区块；② 若暂缓，维持 SDD §2.1 已加的「服务层已落地，端点/UI 为后续」标注 | resolved（端点+UI，2026-09-25：chat-api 新增 `/api/sessions/{id}/commit|versions|share|redeem|revoke|co-presence` 端点，复用 002 库；user-web 会话页版本化抽屉（`SessionVersioningDrawer.vue` + `api/sessionVersioning.ts`，含版本历史/分享/在线成员 + 5s 心跳）已接入 `ChatWindow.vue`，typecheck 通过；新增 5 项单测全绿） |
| `docs/SDD增强功能核验报告.md` §4 缺口 4（001 六层门禁 chat-api 侧未接线 + FR-11 偏离） | 2026-09-25 | DSH Agent（SDD 核验轮） | `harness_config/gate_adapter.py` 注释自认「001 gatekeeper is not yet wired（OQ-3）」，chat-api 运行时工具调用走自有 governance（position_policy/approval_runtime/audit）而非 001 六层链；001 FR-11 要求「复用 `EnterpriseApproval`（`enterprise_capabilities/tools/contracts.py`）且不新建审批表」，实现为 admin-api 自建 `gate_approvals` 集合（状态机语义一致但建表未达标） | ① 001 接线：把 chat-api 工具调用准入切到 001 `Gatekeeper.evaluate`（过渡适配器 `gate_adapter.backend_for` 已预留开关）；② FR-11 二选一：改 spec 接受 `gate_approvals` 自建表（推荐，语义已对齐且 Mongo 无 Redis），或实现复用 `EnterpriseApproval` 状态机 | resolved（门禁挂载+FR-11 拍板，2026-09-25：`turn_admission.run_gate_plan` 已挂；**FR-11 已由用户拍板「接受自建表」**——spec FR-11 改为「复用既有审批状态机（`EnterpriseApproval` 或 admin-api 自建 `gate_approvals`，语义等价即可），不新建第二张审批表」，admin-api 侧使用 `gate_approvals`，chat-api 侧复用 011 的 `marked_low_quality` 共享位 + `position_role_audit_logs` 审计流；spec.md Clarify OQ-1 同步更新） |
| `docs/SDD增强功能核验报告.md` §4 缺口 5（T999 审计生产未接线） | 2026-09-25 | DSH Agent（SDD 核验轮） | `services/feature_audit.py`（012/014/015/016/017/018 事件族）+ `im_gateway/audit.py`（013）+ `dream_cycle/evolution_audit.py`（011）事件族/sink/单测完整，但全部业务模块（friction/mr/runner/router/webhook/kg/memory/capability_assets/skill_lifecycle）无任何一处调用 `record_feature_event`/`record_im_event`/`audit_capture` 等，审计 helper 仅被定义与单测 | ① 各业务模块在关键事件点调用 `record_feature_event(sink=001_audit_writer)` 并补端到端集成测试；② 若暂缓，维持 SDD 对照表已加的「框架就绪、生产未接线」标注 | resolved（审计桥接+业务调用点，2026-09-25：`feature_audit_bridge` 已落地，`emit_feature_event`/`aemit_feature_event` 把 012/014/015/016/017/018 事件路由到 001 `position_role_audit_logs` 流；各特性关键事件点已接 `emit_feature_event`——015 KG `add_node`/`add_edge`→`kg.mutated`、`check_all`→`kg.audited`；017 `promote_to_org`→`memory.promoted`；018 `register`→`asset.registered`/`set_status`→`asset.status.changed`；012 `A2AClient.send`→`a2a.outbound`/`a2a.denied`；014 `BizEntity` 构造→`entity.indexed`；016 admin-api `mark_low_quality`→`skill.quality.marked`（admin-api 独立进程，bridge 不可达时静默跳过）；新增 18 项单测全绿） |

<!-- 新增条目追加到上表；用户拍板后按决定执行并把状态改为 resolved。 -->
