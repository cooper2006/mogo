# Requirements Quality Checklist: 019 harness-elastic-config

**Purpose**: 校验 019（厚/薄 Harness 弹性配置：按场景在重治理与轻执行间切换）spec.md 的需求质量——完整性、清晰度、一致性、边界与歧义。这是"需求的单元测试"，**不校验实现是否正确**。
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)

**Review Ownership**: 审阅人专属的需求质量评审工件。仅当审阅人确认某条需求质量准则达标时才标 `[x]`。
**Marker Semantics**: `[x]` 表示"需求质量已审阅且达标"，**不表示**实现完成。
**生成规则**: 本文件由 `/speckit-checklist` 生成，条目不得预先标 `[x]`；`/speckit-implement` 读取勾选态作为门禁且不得修改标记。

## 完整性（Completeness）

- [ ] CHK001 "厚/薄两种模式"（FR-1）是否定义"厚"与"薄"各自启用的门禁层集合（clarify 已定薄模式可省审批/配额、保留身份/RBAC/脱敏/审计/红线，FR 是否同步该层集合）？
- [ ] CHK002 "按场景/租户/工具维度差异化"（FR-3）是否穷举三个维度各自的配置入口（哪个管理面维护各维度的厚度 profile）？
- [ ] CHK003 "厚度声明式配置 = 门禁层启用/跳过 + 审计粒度 + 超时"（FR-2）是否三者都定义（"审计粒度"有哪些档位，"超时"指哪个超时，spec 是否分别定义）？
- [ ] CHK004 "默认厚模式，降级需显式配置"（FR-8）是否定义默认 profile 的落点（新租户/新场景未配置时默认厚，spec 是否声明默认值）？

## 清晰度（Clarity）

- [ ] CHK005 clarify 已定"维度优先级：场景 > 租户 > 工具"（OQ-1）——FR-3 是否同步该覆盖优先级（还是正文只写"按维度差异化"无优先级）？
- [ ] CHK006 clarify 已定"薄模式可省 = 审批 + 配额；不可省 = 身份 + RBAC + 脱敏 + 审计 + R4 红线"（OQ-2）——FR-5/FR-6 是否同步该"可省/不可省"分层（还是正文只写"省略非红线层"未枚举）？
- [ ] CHK007 clarify 已定"薄模式最小审计 = 身份主体 + 工具名 + 结果 + 时间戳四元组"（OQ-4）——FR-6"审计底线"是否同步该最小集（还是正文只写"审计不可关"无最低粒度）？
- [ ] CHK008 clarify 已定"切换生效时机 = 工具调用级"（OQ-5）——FR-4 是否同步该生效粒度（还是正文写"按场景可用不同厚度"无生效时机）？

## 一致性（Consistency）

- [ ] CHK009 与 001（gatekeeper）"019 是 001 六层门禁的开关层，不改 001 语义"（FR/Non-Goals + clarify OQ-3）——001 是否声明 019 层开关是其六层链的"启用哪几层"配置（001 六层与 019 可省层是否同一"层"概念，001 层 4/5 = 审批/配额 对应 019 可省层）？
- [ ] CHK010 与 009（hooks-interception）"薄模式可跳过非红线 Hooks，fail_closed 底线保留"（009 CHK011 提过）——009 是否声明其非红线钩子可被 019 薄模式跳过（009 钩子与 019 层的"非红线"定义是否一致）？
- [ ] CHK011 与 006（position-rbac）"厚度配置维度/角色"（FR-3 维度）——厚度 profile 的配置权是否受 006 RBAC 约束（谁能配厚度，spec 是否声明）？

## 边界与歧义（Edge cases & Ambiguity）

- [ ] CHK012 clarify 已定"019 先挂 approval_runtime/audit 过渡，001 完成后切到 001 gatekeeper"（OQ-3）——过渡期"001 未完成时 019 层开关挂在哪"是否定义（019 能否在 001 缺位时单独工作）？
- [ ] CHK013 "厚/薄切换"在"同一 Agent 跨场景"（FR-4）时的会话级状态（一个会话中途从厚切薄，进行中的工具调用用哪种厚度）是否定义？
- [ ] CHK014 厚度配置变更"留审计"（FR-7）的审计粒度（变更前后 profile 全量 vs 变更字段 diff，spec 是否定义审计内容）是否清晰？
- [ ] CHK015 "薄模式省略层"的配置错误（误配省略了红线层）时是否有"配置校验拒绝对红线层做省略"的守护（clarify OQ-2 定红线不可省，spec 是否声明配置侧的守护/拒绝）？

## Notes

- 本清单为需求质量门禁，全部条目需审阅人逐条评估后勾选；未勾选项构成 `/speckit-implement` 的拦截门禁。
- 标 `[x]` 仅代表"需求质量达标"，不代表实现完成。
- P2 后置特性，CHK 项可在 019 排期前集中处理。
- **CHK009 是关键一致性点**：019 的"可省层"与 001 的"六层"必须同一层概念，001 层 4/5（审批/配额）= 019 可省层，需与 001 spec 双向确认；clarify 已定值（CHK005–CHK008）需确认是否已回填 FR 正文。
