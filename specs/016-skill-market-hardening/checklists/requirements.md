# Requirements Quality Checklist: 016 skill-market-hardening

**Purpose**: 校验 016（Skill 市场强化：调用监控/效果打分/灰度回滚/低质量标记）spec.md 的需求质量——完整性、清晰度、一致性、边界与歧义。这是"需求的单元测试"，**不校验实现是否正确**。
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)

**Review Ownership**: 审阅人专属的需求质量评审工件。仅当审阅人确认某条需求质量准则达标时才标 `[x]`。
**Marker Semantics**: `[x]` 表示"需求质量已审阅且达标"，**不表示**实现完成。
**生成规则**: 本文件由 `/speckit-checklist` 生成，条目不得预先标 `[x]`；`/speckit-implement` 读取勾选态作为门禁且不得修改标记。

## 完整性（Completeness）

- [ ] CHK001 调用监控（FR-1）四指标（量/成功率/耗时/异常）是否都定义了统计口径（"异常"含哪些状态，成功率与异常率是否互补，时间粒度是分钟/小时/日）？
- [ ] CHK002 效果打分（FR-3）三维度（成功率/采纳/纠正）是否定义各自的数据来源（采纳计数哪来、纠正如何判定，clarify 已定权重 0.5/0.3/0.2，FR 是否同步权重与维度来源）？
- [ ] CHK003 灰度发布（FR-4）"按比例/按用户"是否定义灰度桶分配机制（同一用户是否稳定落在同一桶，避免抖动）？
- [ ] CHK004 低质量标记（FR-6）"标记/降权"是否定义降权的用户可见行为（市场列表隐藏 vs 排序靠后 vs 仍可搜索，spec 写"降权"无细则）？

## 清晰度（Clarity）

- [ ] CHK005 clarify 已定"效果分 = 成功率 0.5 + 采纳率 0.3 + 纠正率反向 0.2，权重可配"（OQ-1）——FR-3 是否同步该口径（还是正文只写"综合打分"无权重）？
- [ ] CHK006 clarify 已定"灰度按租户（首期）"（OQ-2）——FR-4 是否同步"首期按租户"（还是正文写"按比例/用户"与 clarify 的"按租户"矛盾）？
- [ ] CHK007 clarify 已定"异常率 >20% 自动回滚"（OQ-3）——FR-5 是否同步该阈值与"可配"（还是正文只写"异常回滚"无阈值）？
- [ ] CHK008 clarify 已定"效果分 <0.4 持续 7 天标记低质量"（OQ-4）——FR-6 是否同步该阈值（还是正文无量化）？

## 一致性（Consistency）

- [ ] CHK009 与 004（skillhub-lifecycle）"共用版本模型，016 是市场侧叠加层"（FR-9/Non-Goals）——004 是否声明 016 的监控/灰度/标记是其市场的强化（004 CHK013 提过 004/016 边界互斥，是否对齐）？
- [ ] CHK010 与 011（dream-cycle）"共用同一低质量标记位 skill_status.marked_low_quality，避免双写"（clarify OQ-5）——011 是否声明与 016 共用该标记位（011 CHK011 提过，两边是否一致）？
- [ ] CHK011 "调用监控数据源复用既有审计/事件通道"（Notes）——调用指标具体取自 token_usage_logs 还是审计事件（与 007/008 的数据源口径是否一致，008 用 token_usage_logs）？

## 边界与歧义（Edge cases & Ambiguity）

- [ ] CHK012 灰度中"异常率 >20%"的样本量门槛（调用量过小时 20% 无意义，clarify 未定最小样本量）是否定义？
- [ ] CHK013 回滚后"稳定版本"如何判定（上一发布版本 vs 指定版本，clarify 未定）是否定义？
- [ ] CHK014 低质量标记后"可人工恢复"（Success 隐含）的恢复路径与恢复后是否重置低质量窗口（spec 未提）是否定义？
- [ ] CHK015 同一 Skill 多版本并行灰度时的指标归集（监控按版本维度，多版本同时在跑如何分别统计）是否清晰？

## Notes

- 本清单为需求质量门禁，全部条目需审阅人逐条评估后勾选；未勾选项构成 `/speckit-implement` 的拦截门禁。
- 标 `[x]` 仅代表"需求质量达标"，不代表实现完成。
- P2 后置特性，CHK 项可在 016 排期前集中处理。
- **CHK006 是明确矛盾点**：spec FR-4 写"按比例/按用户"，clarify OQ-2 定"首期按租户"，二者口径冲突，需审阅时修正 spec 使其与 clarify 一致。
- clarify 已定值（CHK005/CHK007/CHK008）需确认是否已回填 FR 正文；CHK009/CHK010 需与 004/011 口径统一。
