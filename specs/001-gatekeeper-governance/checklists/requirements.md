# Requirements Quality Checklist: 001 gatekeeper-governance

**Purpose**: 校验 001（六层门禁治理与风控）spec.md 的需求质量——完整性、清晰度、一致性、边界与歧义。这是"需求的单元测试"，**不校验实现是否正确**。
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)

**Review Ownership**: 审阅人专属的需求质量评审工件。仅当审阅人确认某条需求质量准则达标时才标 `[x]`。
**Marker Semantics**: `[x]` 表示"需求质量已审阅且达标"，**不表示**实现完成。
**生成规则**: 本文件由 `/speckit-checklist` 生成，条目不得预先标 `[x]`；`/speckit-implement` 读取勾选态作为门禁且不得修改标记。

## 完整性（Completeness）

- [ ] CHK001 六层门禁链（身份→RBAC→脱敏→审批→配额→审计）的每层是否有独立的失败/拒绝语义定义（FR-1 只写"任一层拒绝短路"，未逐层定义拒绝时的返回码/事件）？
- [ ] CHK002 R0–R4 五级 + L1–L5 五级 = 25 格矩阵（FR-3），spec 是否给出矩阵的**完整取值表**还是仅 3 个示例（Acceptance 只举 L3×R2/L5×R4/R0）？
- [x] CHK003 PII 类型（私钥/身份证/银行卡/手机号/邮箱）是否穷举？是否遗漏（IP/地址/姓名/订单号等）？
- [ ] CHK004 权限码"三级隔离（租户/组织/用户）"（FR-5）是否定义级联语义（上级是否含下级，用户级未授权时是否回落组织级）？
- [ ] CHK005 配额时间窗口（每分钟/每天/每月，FR-8）是否定义"跨窗口"的计数重置与边界时刻（时区/UTC）？

## 清晰度（Clarity）

- [ ] CHK006 "未知权限码 → fail-closed 拒绝"（FR-5）是否定义"未知"的判定（拼写错误 vs 未注册的合法码，二者是否都拒）？
- [ ] CHK007 "岗位角色作为权限码预设组"（FR-6）是否定义预设组的存储结构（角色→权限码列表映射，是否可部分覆盖）？
- [x] CHK008 "脱敏结果保留可追溯"（US3）中"哈希可对照"是否澄清为"仅审计指纹，不存明文"（spec 已声明，是否有可验证判定）？
- [ ] CHK009 clarify 已定审批"poll + 5min 超时"（OQ-1）——spec 正文 FR 是否同步登记了 5min 默认值（还是只在 Clarify 记录里，正文未更新）？
- [ ] CHK010 clarify 已定配额用 MongoDB（OQ-2）——FR-8 是否声明存储介质（还是仍写"计数"而介质仅存于 clarify 记录）？

## 一致性（Consistency）

- [x] CHK011 Non-Goals 写"不实现 LLM 网关韧性（属特性 002）"，但 002 实为 session-versioning、LLM 网关是 007——特性编号引用是否错配（002/003/004/005 在 Non-Goals 里指代是否对得上实际编号）？
- [ ] CHK012 与 006（position-rbac）"岗位角色→权限码预设组"的口径是否一致（006 是岗位角色模型，001 是细粒度权限码，二者映射关系是否在两个 spec 双向声明）？
- [ ] CHK013 与 019（harness-elastic）"薄模式可省审批/配额"是否一致（019 的层开关与 001 六层链是否同一"层"概念）？
- [ ] CHK014 "脱敏层不改变工具执行语义"（Notes）与"脱敏结果进工具请求体"（US3 私有密钥不出现在请求体）是否矛盾（脱敏到底在请求体前还是审计侧）？

## 边界与歧义（Edge cases & Ambiguity）

- [x] CHK015 配额计数器并发竞态（高并发下原子 `findOneAndUpdate`）是否在 spec 级要求（还是实现细节）？
- [ ] CHK016 R4 红线"管理员不可覆盖"（FR-4）的"管理员"边界是否定义（全能力管理员能否强制 allow，spec 写不可，是否需明确到角色）？
- [ ] CHK017 六层"可通过声明式配置增删层"（FR-10）与"任一层可短路"是否冲突（删掉某层后短路语义如何保持）？
- [ ] CHK018 审批 5min 超时后的降级行为（超时是自动拒绝还是保持挂起）是否明确（clarify OQ-1 写了超时值但未写超时后动作）？

## Notes

- 本清单为需求质量门禁，全部条目需审阅人逐条评估后勾选；未勾选项构成 `/speckit-implement` 的拦截门禁。
- 标 `[x]` 仅代表"需求质量达标"，不代表实现完成。
## 评审结论（2026-07-08 勾选，agent 代审）

**达标已勾 `[x]`（4 项）**：CHK003（PII 5 类已锁定）、CHK008（脱敏仅存审计指纹已声明）、CHK011（编号已订正 007/010/011/002）、CHK015（并发竞态属实现细节，spec 正确不定义）。

**未勾 `[ ]` = 真实 spec 缺口（14 项，需回填 spec 后才能勾选）：**
- **clarify 已定但 FR 正文未回填**：CHK009（审批 5min 未进 FR）、CHK010（配额 MongoDB 未进 FR-8）
- **未定义的语义/边界**：CHK001（逐层拒绝返回码）、CHK002（25 格完整取值表）、CHK004（权限码级联/回落语义）、CHK005（配额跨窗口重置 + 时区）、CHK006（"未知权限码"判定细分）、CHK007（预设组存储结构）、CHK016（R4 管理员边界到角色）、CHK017（增删层 vs 短路冲突）、CHK018（5min 超时后动作）
- **跨特性待对齐**：CHK012（001↔006 双向声明）、CHK013（001↔019 层概念）、CHK014（脱敏在请求体前 vs 审计侧）

> 这 14 项是 001 进 implement 前需先消解的需求缺口。建议：先回填 FR 正文（CHK009/010）+ 补矩阵取值表（CHK002）+ 定义级联/时区/预设组/超时动作，再逐项勾选。
