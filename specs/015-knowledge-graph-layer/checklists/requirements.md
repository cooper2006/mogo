# Requirements Quality Checklist: 015 knowledge-graph-layer

**Purpose**: 校验 015（知识图谱层：实体/关系抽取、多跳推理、一致性校验）spec.md 的需求质量——完整性、清晰度、一致性、边界与歧义。这是"需求的单元测试"，**不校验实现是否正确**。
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)

**Review Ownership**: 审阅人专属的需求质量评审工件。仅当审阅人确认某条需求质量准则达标时才标 `[x]`。
**Marker Semantics**: `[x]` 表示"需求质量已审阅且达标"，**不表示**实现完成。
**生成规则**: 本文件由 `/speckit-checklist` 生成，条目不得预先标 `[x]`；`/speckit-implement` 读取勾选态作为门禁且不得修改标记。

## 完整性（Completeness）

- [ ] CHK001 实体/关系类型（FR-1/FR-7）是否穷举（clarify 已定"人/组织/产品/事件 + 隶属/负责/引用/关联"，FR 正文是否同步这套 schema，还是正文只写"实体/关系"无类型）？
- [ ] CHK002 "多跳推理"（FR-4）是否定义"跳"的语义（一次关系遍历 = 一跳，clarify 已定默认 3 跳，FR 是否声明跳数上限）？
- [ ] CHK003 "一致性约束"（FR-6）是否枚举约束种类（clarify 已定互斥/传递/基数，FR 正文是否同步三类，还是正文只写"检测矛盾"无种类）？
- [ ] CHK004 图谱"可查询实体邻居/可遍历路径"（FR-2 隐含）是否定义查询入口（按实体 ID 查邻居 vs 按关系类型查，查询接口 spec 是否声明）？

## 清晰度（Clarity）

- [ ] CHK005 clarify 已定"首期 MongoDB 邻接，节点 >50 万 或 查询 p95 >500ms 迁 Neo4j"（OQ-1）——FR/Non-Goals"量大再迁"是否同步该迁移阈值（还是正文只写"量大"无量化）？
- [ ] CHK006 "路径断裂明确标注，不臆测"（FR-5/Success）是否定义"断裂"的判定（某跳关系缺失即断，标注形式是"图谱无此关系"还是列出断在哪跳）？
- [ ] CHK007 "实体 schema 可配置"（FR-7）的配置粒度（按领域扩展实体类型/关系类型，谁配置，配置存哪）是否清晰？
- [ ] CHK008 "增量抽取合并，不破坏既有图谱"（FR-3 隐含）是否定义合并策略（同实体多来源属性是覆盖/合并/冲突标记，spec 未明）？

## 一致性（Consistency）

- [ ] CHK009 与 005（knowledge-rag）"图谱与 RAG 并行、结果可融合"（FR-9/Non-Goals）——005 是否声明其检索结果可与图谱推理结果融合（同 014 CHK009 口径）？
- [ ] CHK010 与 014（business-semantic-index）"图谱节点可引用 biz_entities（指针引用，不复制数据）"（clarify OQ-5）——014 是否声明其实体可被图谱指针引用（014/015 双向对齐）？
- [ ] CHK011 与 001（gatekeeper）"图谱访问受 001 权限约束"（FR-8）——图谱查询的权限码（`kg:read` 类）是否在 001 权限码模型中声明？

## 边界与歧义（Edge cases & Ambiguity）

- [ ] CHK012 多跳查询命中环（A→B→A）时的行为（是否防环，跳数上限 clarify 已定 3 跳，环是否截断）是否定义？
- [ ] CHK013 "实体抽取"从文档来的准确率边界（抽取错误的实体入图，是否有"低置信实体不入库"门槛，spec 未提抽取置信度）？
- [ ] CHK014 矛盾检测（FR-6）命中后的处置（仅标记 vs 阻断查询 vs 提示，spec 写"标记冲突"但未定标记后的可查询性）是否定义？
- [ ] CHK015 图谱与 RAG 结果冲突时（文本检索命中 vs 图谱推理结果不一致）的融合裁决规则（FR-9"可融合"未定冲突仲裁）是否定义？

## Notes

- 本清单为需求质量门禁，全部条目需审阅人逐条评估后勾选；未勾选项构成 `/speckit-implement` 的拦截门禁。
- 标 `[x]` 仅代表"需求质量达标"，不代表实现完成。
- P2 后置特性，CHK 项可在 015 排期前集中处理。
- clarify 已定值（CHK001/CHK002/CHK003/CHK005）需确认是否已回填 FR 正文；CHK009/CHK010 需与 005/014 口径统一。
