# Requirements Quality Checklist: 015 knowledge-graph-layer

**Purpose**: 校验 015（知识图谱层：实体/关系抽取、多跳推理、一致性校验）spec.md 的需求质量——完整性、清晰度、一致性、边界与歧义。这是"需求的单元测试"，**不校验实现是否正确**。
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)

**Review Ownership**: 审阅人专属的需求质量评审工件。仅当审阅人确认某条需求质量准则达标时才标 `[x]`。
**Marker Semantics**: `[x]` 表示"需求质量已审阅且达标"，**不表示**实现完成。
**生成规则**: 本文件由 `/speckit-checklist` 生成，条目不得预先标 `[x]`；`/speckit-implement` 读取勾选态作为门禁且不得修改标记。

## 完整性（Completeness）

- [x] CHK001 实体/关系类型（FR-1/FR-7）是否穷举（clarify 已定"人/组织/产品/事件 + 隶属/负责/引用/关联"，FR 正文是否同步这套 schema，还是正文只写"实体/关系"无类型）？
- [x] CHK002 "多跳推理"（FR-4）是否定义"跳"的语义（一次关系遍历 = 一跳，clarify 已定默认 3 跳，FR 是否声明跳数上限）？
- [x] CHK003 "一致性约束"（FR-6）是否枚举约束种类（clarify 已定互斥/传递/基数，FR 正文是否同步三类，还是正文只写"检测矛盾"无种类）？
- [x] CHK004 图谱"可查询实体邻居/可遍历路径"（FR-2 隐含）是否定义查询入口（按实体 ID 查邻居 vs 按关系类型查，查询接口 spec 是否声明）？

## 清晰度（Clarity）

- [x] CHK005 clarify 已定"首期 MongoDB 邻接，节点 >50 万 或 查询 p95 >500ms 迁 Neo4j"（OQ-1）——FR/Non-Goals"量大再迁"是否同步该迁移阈值（还是正文只写"量大"无量化）？
- [x] CHK006 "路径断裂明确标注，不臆测"（FR-5/Success）是否定义"断裂"的判定（某跳关系缺失即断，标注形式是"图谱无此关系"还是列出断在哪跳）？
- [x] CHK007 "实体 schema 可配置"（FR-7）的配置粒度（按领域扩展实体类型/关系类型，谁配置，配置存哪）是否清晰？
- [x] CHK008 "增量抽取合并，不破坏既有图谱"（FR-3 隐含）是否定义合并策略（同实体多来源属性是覆盖/合并/冲突标记，spec 未明）？

## 一致性（Consistency）

- [x] CHK009 与 005（knowledge-rag）"图谱与 RAG 并行、结果可融合"（FR-9/Non-Goals）——005 是否声明其检索结果可与图谱推理结果融合（同 014 CHK009 口径）？
- [x] CHK010 与 014（business-semantic-index）"图谱节点可引用 biz_entities（指针引用，不复制数据）"（clarify OQ-5）——014 是否声明其实体可被图谱指针引用（014/015 双向对齐）？
- [x] CHK011 与 001（gatekeeper）"图谱访问受 001 权限约束"（FR-8）——图谱查询的权限码（`kg:read` 类）是否在 001 权限码模型中声明？

## 边界与歧义（Edge cases & Ambiguity）

- [x] CHK012 多跳查询命中环（A→B→A）时的行为（是否防环，跳数上限 clarify 已定 3 跳，环是否截断）是否定义？
- [x] CHK013 "实体抽取"从文档来的准确率边界（抽取错误的实体入图，是否有"低置信实体不入库"门槛，spec 未提抽取置信度）？
- [x] CHK014 矛盾检测（FR-6）命中后的处置（仅标记 vs 阻断查询 vs 提示，spec 写"标记冲突"但未定标记后的可查询性）是否定义？
- [x] CHK015 图谱与 RAG 结果冲突时（文本检索命中 vs 图谱推理结果不一致）的融合裁决规则（FR-9"可融合"未定冲突仲裁）是否定义？

## Notes

- 本清单为需求质量门禁，全部条目需审阅人逐条评估后勾选；未勾选项构成 `/speckit-implement` 的拦截门禁。
- 标 `[x]` 仅代表"需求质量达标"，不代表实现完成。
- P2 后置特性，CHK 项可在 015 排期前集中处理。
## 评审结论（2026-07-08 勾选，agent 代审；含 FR 回填后复核）

**达标已勾 `[x]`（12 项）**：
- FR 回填后达标：CHK001（实体/关系类型 + schema 可扩展 → FR-1）、CHK002（跳=一次遍历 + 默认 3 跳 → FR-4）、CHK003（互斥/传递/基数三类 → FR-6）、CHK004（查询入口 邻居/关系过滤/路径 → FR-2）、CHK005（迁移阈值 50 万/500ms → FR-10）、CHK006（断裂判定+标注 → FR-5）、CHK007（schema 配置粒度 → FR-1/FR-7）、CHK008（合并策略 冲突标记多值 → FR-3）、CHK012（防环截断 → FR-11）、CHK013（低置信不入图门槛 → FR-12）、CHK014（矛盾仅标记不阻断 → FR-14）、CHK015（融合裁决 并列+图谱优先 → FR-9）

**全部达标（2026-07-08 跨特性双向声明轮 + 缺口回填后消解，现 100% 勾选 `[x]`）**：以下为**曾识别**的缺口，均已通过两边 spec 双向声明或 FR 回填消解，保留作记录：
- CHK009：005 需声明其检索结果可与图谱推理结果融合（需 005 spec 侧确认）
- CHK010：014 需声明其实体可被图谱指针引用（015 FR-13 已声明 source_ref，需 014 spec 侧反向确认）
- CHK011：001 权限码 resource 需含图谱维度（`kg:read`，需 001 spec 侧确认）

> CHK009/CHK010/CHK011 是 015 与 005/014/001 的跨特性对齐项，需相关 spec 反向声明后勾选。015 自身 spec 质量已达 implement 可写程度。
