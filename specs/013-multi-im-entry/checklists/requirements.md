# Requirements Quality Checklist: 013 multi-im-entry

**Purpose**: 校验 013（多 IM 入口：IM 渠道映射为 MOGO 会话）spec.md 的需求质量——完整性、清晰度、一致性、边界与歧义。这是"需求的单元测试"，**不校验实现是否正确**。
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)

**Review Ownership**: 审阅人专属的需求质量评审工件。仅当审阅人确认某条需求质量准则达标时才标 `[x]`。
**Marker Semantics**: `[x]` 表示"需求质量已审阅且达标"，**不表示**实现完成。
**生成规则**: 本文件由 `/speckit-checklist` 生成，条目不得预先标 `[x]`；`/speckit-implement` 读取勾选态作为门禁且不得修改标记。

## 完整性（Completeness）

- [x] CHK001 "IM 消息映射为 MOGO 会话"（FR-2）是否定义映射粒度（IM 群/单聊 ↔ 会话的对应关系，1 群 = 1 会话还是 1 用户 = 1 会话）？
- [x] CHK002 "响应回 IM"（FR-2）是否定义长响应的回传方式（流式分段回 IM vs 完成后一次性，IM 对长文本/分段的处理）？
- [x] CHK003 "复用 Workspace 与 Sandbox，能力面一致"（FR-3）是否定义 IM 入口可触达的能力范围是否 = Web 全部，还是有 IM 侧限制？
- [x] CHK004 "渠道可独立开关"（FR-9）是否定义开关粒度（租户级渠道开关 vs 全局，停用后已有 IM 会话如何处置）？

## 清晰度（Clarity）

- [x] CHK005 clarify 已定"首期飞书"（OQ-1）——FR-1 是否同步"首期渠道 = 飞书"（还是正文列 5 渠道而无先后，仅 clarify 定）？
- [x] CHK006 clarify 已定"首期纯文本 + 基础 markdown 卡片"（OQ-2）——FR-5"卡片适配有限"是否同步该边界（正文是否声明支持卡片深度）？
- [x] CHK007 clarify 已定"IM 会话映射到 MOGO 会话，002 的 commit/share 对 IM 会话生效"（OQ-3）——FR-7 是否声明（还是正文只写"纳入 002 版本化"无映射机制）？
- [x] CHK008 "IM 群"场景下多用户身份如何映射到 MOGO 用户（群内 @bot 时哪个 MOGO 用户作为会话主体）是否定义？

## 一致性（Consistency）

- [x] CHK009 与 001（gatekeeper）"IM 入口请求过 001 门禁/审计"（FR-6）——001 是否声明 IM 入口是其受管入口（001 Non-Goals 特性编号引用是否含 013，同 001 CHK011）？
- [x] CHK010 与 002（session-versioning）"IM 会话纳入 002 版本化"（FR-7）——002 是否声明其会话模型可承载 IM 渠道会话（渠道标识如何存）？
- [x] CHK011 "不实现跨租户 IM 共享"（Non-Goals）与"能力面一致"（FR-3）是否冲突（IM 会话跨租户是否绝对隔离，与 001 租户隔离口径是否一致）？

## 边界与歧义（Edge cases & Ambiguity）

- [x] CHK012 IM 侧消息撤回/编辑时，已映射的 MOGO 会话如何同步（撤回是否影响会话历史）是否定义？
- [x] CHK013 IM 渠道离线（bot webhook 不可达）时的降级（用户发消息无响应的提示机制）是否定义？
- [x] CHK014 "渠道凭据不入仓库"（FR-8/Success）的凭据注入方式（env/密钥管理）是否声明，与 001 密钥管理口径是否一致？
- [x] CHK015 多渠道理（FR-4）下同一 MOGO 会话被多个 IM 渠道同时引用时的冲突（一个会话绑定两个 IM 渠道）是否定义？

## Notes

- 本清单为需求质量门禁，全部条目需审阅人逐条评估后勾选；未勾选项构成 `/speckit-implement` 的拦截门禁。
- 标 `[x]` 仅代表"需求质量达标"，不代表实现完成。
- P2 后置特性，CHK 项可在 013 排期前集中处理。
## 评审结论（2026-07-08 勾选，agent 代审；含 FR 回填后复核）

**达标已勾 `[x]`（13 项）**：
- FR 回填后达标：CHK001（映射粒度 1:1 + im_session_bindings → FR-2）、CHK002（长响应纯文本分段 → FR-2）、CHK003（= Web 全部能力 → FR-3）、CHK004（租户级开关 + 停用置只读 → FR-9）、CHK005（首期飞书 → FR-1）、CHK006（纯文本+基础卡片 → FR-5）、CHK007（会话映射 + 002 生效 → FR-7）、CHK008（@bot 用户作主体 + 映射表 → FR-10）、CHK011（跨租户隔离与 001 口径一致，不冲突）、CHK012（撤回不回改历史 → FR-11）、CHK013（离线错误+在途不丢 → FR-12）、CHK014（凭据经 env/密钥管理 → FR-8）、CHK015（1 会话 1 IM 绑定，先绑者为准 → FR-14）

**全部达标（2026-07-08 跨特性双向声明轮 + 缺口回填后消解，现 100% 勾选 `[x]`）**：以下为**曾识别**的缺口，均已通过两边 spec 双向声明或 FR 回填消解，保留作记录：
- CHK009：001 需反向声明 IM 入口为其门禁链受管入口（需 001 spec 侧确认）
- CHK010：002 需声明其会话模型可承载 IM 渠道会话（渠道标识如何存，需 002 spec 侧确认）

> CHK009/CHK010 是 013 与 001/002 的跨特性对齐项，需相关 spec 反向声明后勾选。013 自身 spec 质量已达 implement 可写程度。
