# Requirements Quality Checklist: 013 multi-im-entry

**Purpose**: 校验 013（多 IM 入口：IM 渠道映射为 MOVO 会话）spec.md 的需求质量——完整性、清晰度、一致性、边界与歧义。这是"需求的单元测试"，**不校验实现是否正确**。
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)

**Review Ownership**: 审阅人专属的需求质量评审工件。仅当审阅人确认某条需求质量准则达标时才标 `[x]`。
**Marker Semantics**: `[x]` 表示"需求质量已审阅且达标"，**不表示**实现完成。
**生成规则**: 本文件由 `/speckit-checklist` 生成，条目不得预先标 `[x]`；`/speckit-implement` 读取勾选态作为门禁且不得修改标记。

## 完整性（Completeness）

- [ ] CHK001 "IM 消息映射为 MOVO 会话"（FR-2）是否定义映射粒度（IM 群/单聊 ↔ 会话的对应关系，1 群 = 1 会话还是 1 用户 = 1 会话）？
- [ ] CHK002 "响应回 IM"（FR-2）是否定义长响应的回传方式（流式分段回 IM vs 完成后一次性，IM 对长文本/分段的处理）？
- [ ] CHK003 "复用 Workspace 与 Sandbox，能力面一致"（FR-3）是否定义 IM 入口可触达的能力范围是否 = Web 全部，还是有 IM 侧限制？
- [ ] CHK004 "渠道可独立开关"（FR-9）是否定义开关粒度（租户级渠道开关 vs 全局，停用后已有 IM 会话如何处置）？

## 清晰度（Clarity）

- [ ] CHK005 clarify 已定"首期飞书"（OQ-1）——FR-1 是否同步"首期渠道 = 飞书"（还是正文列 5 渠道而无先后，仅 clarify 定）？
- [ ] CHK006 clarify 已定"首期纯文本 + 基础 markdown 卡片"（OQ-2）——FR-5"卡片适配有限"是否同步该边界（正文是否声明支持卡片深度）？
- [ ] CHK007 clarify 已定"IM 会话映射到 MOVO 会话，002 的 commit/share 对 IM 会话生效"（OQ-3）——FR-7 是否声明（还是正文只写"纳入 002 版本化"无映射机制）？
- [ ] CHK008 "IM 群"场景下多用户身份如何映射到 MOVO 用户（群内 @bot 时哪个 MOVO 用户作为会话主体）是否定义？

## 一致性（Consistency）

- [ ] CHK009 与 001（gatekeeper）"IM 入口请求过 001 门禁/审计"（FR-6）——001 是否声明 IM 入口是其受管入口（001 Non-Goals 特性编号引用是否含 013，同 001 CHK011）？
- [ ] CHK010 与 002（session-versioning）"IM 会话纳入 002 版本化"（FR-7）——002 是否声明其会话模型可承载 IM 渠道会话（渠道标识如何存）？
- [ ] CHK011 "不实现跨租户 IM 共享"（Non-Goals）与"能力面一致"（FR-3）是否冲突（IM 会话跨租户是否绝对隔离，与 001 租户隔离口径是否一致）？

## 边界与歧义（Edge cases & Ambiguity）

- [ ] CHK012 IM 侧消息撤回/编辑时，已映射的 MOVO 会话如何同步（撤回是否影响会话历史）是否定义？
- [ ] CHK013 IM 渠道离线（bot webhook 不可达）时的降级（用户发消息无响应的提示机制）是否定义？
- [ ] CHK014 "渠道凭据不入仓库"（FR-8/Success）的凭据注入方式（env/密钥管理）是否声明，与 001 密钥管理口径是否一致？
- [ ] CHK015 多渠道理（FR-4）下同一 MOVO 会话被多个 IM 渠道同时引用时的冲突（一个会话绑定两个 IM 渠道）是否定义？

## Notes

- 本清单为需求质量门禁，全部条目需审阅人逐条评估后勾选；未勾选项构成 `/speckit-implement` 的拦截门禁。
- 标 `[x]` 仅代表"需求质量达标"，不代表实现完成。
- P2 后置特性，CHK 项可在 013 排期前集中处理。
- **CHK009 关键一致性点**：001 Non-Goals 特性编号引用错配（同 001 CHK011）会连带影响 013 与 001 的关系声明，需审阅核实。
