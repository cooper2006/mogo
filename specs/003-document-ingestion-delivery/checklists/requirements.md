# Requirements Quality Checklist: 003 document-ingestion-delivery

**Purpose**: 校验 003（多模态文档解析与内容交付物）spec.md 的需求质量——完整性、清晰度、一致性、边界与歧义。这是"需求的单元测试"，**不校验实现是否正确**。
**Created**: 2026-07-08
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md)

**Review Ownership**: 审阅人专属的需求质量评审工件。仅当审阅人确认某条需求质量准则达标时才标 `[x]`。
**Marker Semantics**: `[x]` 表示"需求质量已审阅且达标"，**不表示**实现完成。
**生成规则**: 本文件由 `/speckit-checklist` 生成，条目不得预先标 `[x]`；`/speckit-implement` 读取勾选态作为门禁且不得修改标记。

## 完整性（Completeness）

- [ ] CHK001 支持格式集合（FR-1 列 PDF/DOCX/XLSX/XLSM/PPTX/CSV/TXT/MD/JSON/图片）是否穷举？是否遗漏常见业务格式（如 ODS/ODP/EPUB）？若有意不列，是否说明理由？
- [ ] CHK002 每个格式是否有明确的成功/失败定义（FR-1 写"不支持的格式任务失败且错误明确"，但未列出"支持"清单与"不支持"清单的精确边界）？
- [ ] CHK003 解析产物契约（FR-2 结构化 Markdown + 表格 + 图片 OCR）是否覆盖"图文混排"场景（图注/图表说明如何进 Markdown）？
- [ ] CHK004 引用锚点（FR-3/FR-4）是否覆盖"表格内引用"与"图表引用"两类锚点，还是仅文档级？
- [ ] CHK005 预览转换（FR-5）是否定义"转换成功后预览 PDF 的留存周期"与"失败原因码枚举"？
- [ ] CHK006 交付物生成（FR-6）是否穷举每种格式产物的验收标准（PPTX 可打开/XLSX 表格完整/DOCX 排版保持/PDF 生成）？
- [ ] CHK007 是否定义了"空文档/超大文档/加密文档"三类边界输入的规格要求（spec 目前未提）？

## 清晰度（Clarity）

- [ ] CHK008 "结构化 Markdown + 表格结构保留"中的"结构化"是否有可判定标准（如表格用 GFM 表格语法，而非拍平文本）？
- [ ] CHK009 FR-4"锚点缺失时降级为文档级引用"的"缺失"判定标准是否明确（什么情况下算缺失）？
- [ ] CHK010 FR-11"Docling 未安装时子回退解析器"的覆盖集与"其余格式报明确错误"是否一一枚举（避免"其余"成模糊兜底）？
- [ ] CHK011 "产物留存策略留白给 001"是否在 spec 中显式标注为"非目标 + 依赖 001 交付"，避免审阅期误以为 003 需自带留存？
- [ ] CHK012 用户故事 4"翻译任务支持 PDF/DOCX/XLSX/TXT/MD 源，二进制格式保持原排版"中"保持原排版"是否有可验证的判定（如页面对齐/样式保留比例）？

## 一致性（Consistency）

- [ ] CHK013 spec 的 Non-Goals（不重做 RAG/不引入新格式）与 FR 是否矛盾（FR 是否意外引入 RAG 实现要求）？
- [ ] CHK014 FR-10"检索与产物访问遵循租户/组织隔离"与 005（knowledge-rag）的访问策略是否口径一致（同指 retrieval_access_policy）？
- [ ] CHK015 spec"与特性 001 审计联动"与 001 尚未实现的事实是否一致（003 是否假设 001 已交付，存在依赖顺序风险）？
- [ ] CHK016 用户故事 5"装配产物与规格声明的格式一致"与 FR-8 是否同义重复但表述不一（"装配"与"生成"边界是否清晰）？

## 边界与歧义（Edge cases & Ambiguity）

- [ ] CHK017 多语言文档（中英混排/纯外文）的解析与 OCR 是否在规格范围内（spec 未提语言维度）？
- [ ] CHK018 同一文档多次上传/更新时，RAG 索引是覆盖还是版本化（与 002 会话版本化是否相关）？
- [ ] CHK019 预览转换依赖 LibreOffice，"无 LibreOffice 环境"的部署场景（如精简镜像）规格如何要求（FR 写"报明确依赖缺失错误"，但部署方是否必须有 LibreOffice 未定）？
- [ ] CHK020 "图片/图表内容一并可检索"中"图表"指 OCR 识别还是多模态模型理解（spec 未定技术语义，但应定行为语义）？

## Notes

- 本清单为需求质量门禁，全部条目需审阅人逐条评估后勾选；未勾选项构成 `/speckit-implement` 的拦截门禁。
- 标 `[x]` 仅代表"需求质量达标"，不代表实现完成。
- 与 001/002/005 的依赖关系（CHK015/CHK018）建议在 clarify 阶段消解。
